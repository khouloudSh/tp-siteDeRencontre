<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    
    <!--Paramètres de connexion à la base de donnée. Avec le Driver , la Bdd, login, password  -->
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
<!-- ici on définie le mode de connexion a la BD -->
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/bdboka?serverTimezone=UTC"/>
                <property name="username" value="root"/>
                <property name="password" value="abc123..."/>
            </dataSource>
        </environment>
    </environments>
  
  <!--Ajout des différents mappers que l'on va utiliser -->
    
</configuration>
